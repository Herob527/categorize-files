procs:
  docker:
    shell: "docker compose down && docker compose up --build --watch"
  nvim:
    shell: "nvim"
  in_db:
    cwd: src
    shell: docker exec -it categorize-db /bin/bash -c "psql -U myuser -d mydb"
    autostart: false
